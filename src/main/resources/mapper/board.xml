<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop.mtcoding.transferdto.model.board.BoardRepository">
    <select id="findAll" resultType="shop.mtcoding.transferdto.model.board.Board">
        select * from board_tb
    </select>

    <select id="findById" resultType="shop.mtcoding.transferdto.model.board.Board">
        select * from board_tb where id = #{id}
    </select>

    <select id="findByIdJoinUser" resultType="shop.mtcoding.transferdto.dto.BoardDetailOutDto">
        select b.*, 
          u.id user_id, 
          u.username user_username, 
          u.password user_password, 
          u.email user_email, 
          u.created_at user_created_at
          from board_tb b join user_tb u
          on b.user_id = u.id
          where b.id = #{id}
    </select>

    <select id="findByIdJoinUser2" resultType="shop.mtcoding.transferdto.dto.BoardDetailOutDto2">
        select b.*, 
          u.id user_id, 
          u.username user_username, 
          u.password user_password, 
          u.email user_email, 
          u.created_at user_created_at
          from board_tb b join user_tb u
          on b.user_id = u.id
          where b.id = #{id}
    </select>

    <insert id="insert">
        insert into board_tb (title, content, user_id, created_at)
        values(#{title}, #{content}, #{userId}, now())
    </insert>

    <delete id="deleteById" >
        delete from board_tb where id = #{id}
    </delete>

    <update id="updateById" >
        update board_tb set title= #{title}, content= #{content} where id = #{id}
    </update>

</mapper>